# MORPH-Exact / MORPH-Hyper 项目真实性与价值潜力验证报告

验证日期：2026-08-30  
验证对象：`MORPH_Exact_Research_Package.zip`

## 一、最终结论

本项目在“研究原型”层面具备真实、非平凡的技术含量。压缩包不是只有概念文案或预制结果：核心算法能够运行，局部行为商确实经过全状态、全输入检查，主要实验可以重现，公开 `bbara` 基准也与上游数据语义一致。其最有价值的部分，是把“选择候选区域—精确组合—求最小外部行为商—验证—把商重新作为同类型原子参与下一轮组合”做成了闭合的可执行流程，并用三元超边处理二元组合无法显示收益的情形。

但目前只能作出以下分级判断：

| 验证事项 | 结论 | 判断依据 |
|---|---|---|
| 技术工件是否真实存在并可运行 | **通过** | 代码、测试、压力验证和各类实验均可执行 |
| 核心有限状态语义是否按声明精确 | **通过** | 证书检查、独立最小性检查、乘积等价检查及 11,500 组重跑均通过 |
| 包内主要实验数字是否可信 | **基本通过** | 状态数、成功率、根机同构、证书状态和峰值规模均重现；耗时随机器变化 |
| 公开基准是否真实 | **通过** | `bbara.kiss2` 与 ISPRAS 上游文件规范化后逐行一致 |
| “proof-carrying”工件是否可脱离程序独立验签 | **部分通过** | 运行时生成并检查状态映射，但包内账本只保存布尔结果，没有保存映射和摘要 |
| 学术原创性与首创优先权 | **未证实** | 无公开论文、仓库、作者、时间戳或第三方评审；相关工作检索也不充分 |
| 当前是否已达到图灵奖级 | **不成立** | 没有长期影响、外部采用、独立复现、理论突破或学术共同体认可的证据 |
| 是否有高价值研究潜力 | **有，但有条件** | 原型思想清晰且闭环；能否升级取决于符号化、真实系统验证、严格理论和公开评审 |

准确表述应是：

> **这是一个技术上真实、设计上有想法、实验上可复现的证明携带式组合抽象原型；它具有形成重要研究方向的潜力，但尚不是已被验证的重大原创理论，更不是已经具备图灵奖资格的成果。**

## 二、验证范围与方法

本次验证覆盖五个层面：

1. 工件完整性：压缩包目录、哈希清单、代码与结果文件的一致性。
2. 实现真实性：组合、最小化、同态检查、最小性检查、等价检查和引擎选择逻辑的代码审阅。
3. 运行真实性：在隔离的 Python 3.11 环境重新安装并执行测试、压力验证和主要实验。
4. 数据真实性：将包内汇总与 CSV/JSON 原始结果交叉计算，并核对公开基准上游文件。
5. 学术真实性：检索最接近的组合式最小化、自动组合顺序、层级 FSM 分解和证明证书研究，并按 ACM 图灵奖公开标准判断当前成熟度。

“真实性”在本报告中分成两个不同问题：一是代码和实验是否真实；二是作者身份、原创时间和学术优先权是否真实。前者能够通过技术手段验证，后者因压缩包缺少外部身份与时间锚点而不能确认。

## 三、工件完整性与来源真实性

### 3.1 包内完整性通过

压缩包共含 64 个普通文件，其中 `SHA256SUMS.txt` 为其余 63 个文件提供 SHA-256。实际执行校验后为 **63/63 全部通过**。这证明当前解压内容与包内清单一致，没有发现包内文件损坏或清单内外不一致。

该结论只证明“内部一致”，不能证明外部来源。清单没有数字签名、公证时间戳或第三方发布记录，任何能够重建压缩包的人都可以同时重建哈希清单。

### 3.2 公开 `bbara` 基准来源通过

包内 `benchmarks/bbara.kiss2` 声明 4 输入、2 输出、10 个控制状态和 60 个积项。将注释、空行和行尾空白规范化后，它与 [ISPRAS `hdl-benchmarks` 仓库中的 LGSynth91 `bbara.kiss2`](https://raw.githubusercontent.com/ispras/hdl-benchmarks/master/lgsynth91/kiss2/bbara.kiss2) 逐行一致。ISPRAS 仓库也明确把该目录描述为 LGSynth'91 公共基准集合：[仓库说明](https://github.com/ispras/hdl-benchmarks)。

项目的 KISS2 转换器把确定 Mealy 机转换为带一个初始零输出的 Moore 语义，再做可达最小化；本地计算得到 9 状态根机，与报告一致。这里验证的是转换后 Moore 行为，不应表述为原始 Mealy 文件本身只有 9 个控制状态。

### 3.3 项目身份和优先权无法认证

压缩包没有以下材料：作者姓名、许可证、Git 历史、提交哈希、公开仓库地址、论文 DOI、预印本编号、机构信息、签名或第三方归档链接。以项目名、`MORPH-Hyper`、`self-abstracting computation` 和核心术语检索，也没有找到可索引的同名公开成果。

所以无法从该包证明：

- 谁是作者；
- 代码何时首次形成；
- 是否存在更早版本；
- 是否已经投稿或经过同行评审；
- 项目名称和具体组合是否拥有学术优先权。

这不否定代码真实性，但会显著降低其作为学术成果、知识产权证据或奖项材料的可信度。

## 四、实现真实性审阅

### 4.1 核心闭环真实存在

实现不是把目标层级直接写入结果。`oracle_clusters` 只用于实验结束后的召回率统计和轨迹标记，没有进入候选评分或安装决策。实际主流程为：

1. 从当前区域之间的可执行信号依赖产生候选；
2. 对候选构造同步可达乘积；
3. 隐藏区域内部信号，只保留外部接口；
4. 对可达 Moore 机做分区细化最小化；
5. 逐状态、逐输入检查输出保持和转移交换；
6. 用另一套表填充可区分状态算法检查商机最小性；
7. 通过后把商机作为同一个 `Machine` 类型的新区域继续组合。

MORPH-Hyper 还会识别一个原始汇聚节点及两个已成熟生产者，尝试两种二元括号顺序，并以连续两次已验证商的链条原子性提交三元区域。代码与报告描述在这一点上一致。

### 4.2 正确性检查不是只看随机样本

单次商安装的同态检查会遍历原始候选的全部可达状态和全部布尔输入，不是抽样检查。最小性检查使用与分区构造不同的可区分状态对算法。机器等价检查则在两台机器的可达乘积上搜索反例。

附加的反向试验也表明检查器能拒绝人为破坏的状态映射，并能识别两个可达但行为等价的冗余状态。因此“检查器始终返回真”的伪验证情形可以排除。

### 4.3 正确性结论的适用边界

已验证结论只适用于有限、确定、同步、布尔端口、Moore 输出语义。当前实现没有覆盖：

- 无限状态或一般程序语义；
- 异步并发与调度；
- 非确定系统；
- 概率系统；
- 数据类型丰富的符号程序；
- 对任意候选搜索策略的全局最优性。

报告中的两个“定理”是合理的数学论证框架，但目前只是项目文档中的证明概要，没有在 Lean、Coq、Isabelle 等证明系统中机械化，也没有由独立形式化语义实现复核。

## 五、独立复现结果

隔离环境使用 Python 3.11、NumPy 2.4.6、pytest 9.1.1。时间只记录本次机器表现，不用于与包内机器横向比较。

### 5.1 核心测试与压力验证

| 项目 | 本次结果 |
|---|---:|
| 单元/性质测试 | 8/8 通过，2.29 秒 |
| 随机最小化 | 10,000/10,000 通过 |
| 隐藏分裂再合成 | 1,000/1,000 通过 |
| 多层网络对独立单体参考 | 500/500 通过 |
| 压力验证合计 | 11,500/11,500 通过，7.38 秒 |

这足以支持“当前有限模型实现没有发现行为错误”，但随机测试不能替代一般正确性证明。

### 5.2 隐藏层级和公开控制器扩展

重新执行 `scaling` 场景：

- 隐藏奇偶层级深度 4–9：6/6 成功；最大 512 个叶单元，最终 2 状态，层级召回 100%，最大已选原始候选 1,024 状态。
- `bbara` 深度 2–6：5/5 成功；最大 64 个叶单元，最终 9 状态，层级召回 100%，最大已选候选 576 状态，最大投机候选 1,152 状态。
- 所有成功运行的根机均与目标根机规范同构，所有安装证书标记均通过。

报告中的 `2^1023` 是原始局部状态数的笛卡尔积上界，不是程序实际枚举了 `2^1023` 个可达状态。项目文档已对此作出说明；评价结果时必须保留这个限定。

### 5.3 跨控制语义

对奇偶、模 5 计数、模式识别、握手、交通灯、交替位协议和混合控制器各运行 3 个种子，得到：

- 成功 21/21；
- 精确根机 21/21；
- 完整安装证书状态 21/21；
- 隐藏层级召回 21/21 为 100%。

这些结果证明同一算法内核不只接受一个根机。但七类实验共用同一种“隐藏 gauge 递归分裂”构造，因此它们主要验证跨根机语义的一致性，不等价于七类独立真实系统。

### 5.4 自然正向生成系统

重新执行两个未调用逆向隐藏分裂器的正向系统：

| 系统 | 最大实例 | 最终状态 | 结果 |
|---|---:|---:|---|
| 注册 XOR 树 | 255 组件，笛卡尔上界 `2^255` | 256 | 成功，全部局部证书通过 |
| 注册模 3 和树 | 63 组件，笛卡尔上界 `3^63` | 729 | 成功，层级召回 100%，全部局部证书通过 |

小实例与单体参考逐行为等价。大实例依赖局部合同链保证，没有构造完整单体机。这是合理的组合式验证方式，但其可信度仍取决于局部检查器和合同定理，而不是一个额外的全局穷尽检查。

### 5.5 基线对比

当前脚本的 240 次 parity/counter/handshake 基线场景全部重跑完成。报告重点列出的 parity 成功率得到重现：

| 深度 | MORPH | 结构顺序 | 最小优先 | 随机 |
|---:|---:|---:|---:|---:|
| 3 | 100% | 100% | 100% | 100% |
| 4 | 100% | 60% | 60% | 20% |
| 5 | 100% | 0% | 0% | 0% |
| 6 | 100% | 0% | 0% | 0% |

`bbara` 深度 4、3 个种子的 12 次对比也重现为 MORPH 3/3、结构顺序 1/3、最小优先 2/3、随机 0/3。

但该对比不能支持“优于 CADP、Supremica 或其他工业工具”的结论，原因有三：

1. 三个基线只是本项目内部的简单顺序策略，不是外部工具实现。
2. 构造器加入的诱饵边只存在于 `contact_edges`；MORPH 主模式只使用无诱饵的 `signal_edges`，而结构基线使用含诱饵的联系图。该实验能说明语义信号优于被污染的结构元数据，但比较条件并不对称。
3. 隐藏层级基准由已知可逆分裂规则生成，其信号依赖结构与目标层级高度相关。深度 6 的初始 63 条执行边中，32 条直接对应第一层真簇；廉价预筛选的前 8 个候选全部是真簇。实验仍然有效，但“层级完全靠昂贵反事实语义发现”的表述会过强。

### 5.6 可复现性缺口

按照 `pyproject.toml` 执行安装后，主基准脚本首次启动失败，因为 `scripts/run_benchmarks.py` 导入了未声明的 `pandas`。临时补装 pandas 3.0.5 后全部实验正常。核心测试和 `validate_exactness.py` 不受此问题影响。

这是小而真实的打包缺陷，应将结论定为“可复现但不是一条命令的干净复现”。

## 六、证明携带主张的真实性边界

运行时的 `QuotientCertificate` 确实包含：原机摘要、商机摘要和完整状态映射；安装前也确实调用检查器验证。因此从运行过程看，“每次安装有证书依据”成立。

但两个持久化 `certificate_ledger_*.json` 只保存：

- 原始/商状态数等统计；
- `homomorphism_certificate: true`；
- `minimality_certificate: true`。

它们没有保存状态映射、原机/商机表、摘要或可重放证明。也就是说，账本是“验证结果日志”，不是可由第三方独立重放的证书。证明证书研究通常要求系统输出形式化证据文档，并由可信检查器独立检查；可参见 Heath 与 Miller 的[有限状态探索证明证书框架](https://arxiv.org/abs/1507.08716)。

因此当前最准确称呼是：

> **运行时内存中证明携带，发布包中证据摘要化。**

若要把“proof-carrying”作为核心学术卖点，必须持久化每次状态映射、机器摘要、端口映射和最小性见证，并提供只读、独立、可重放的检查器。

## 七、相关工作与原创性边界

本项目没有发明“逐步组合并最小化”。Graf 与 Steffen 在 CAV 1990 已经研究通过分步组合和最小化构造最小系统，并使用接口行为控制中间爆炸：[原论文页面](https://link.springer.com/chapter/10.1007/BFb0023732)。

“自动选择组合顺序”也不是新问题。Crouzen 与 Lang 的 Smart Reduction 会对连通候选聚合计算隐藏率和交错率启发式，自动选择组合顺序，默认还允许一次考虑最多四个 LTS，并已集成 CADP、在多类系统上实验：[论文原文](https://cadp.inria.fr/ftp/publications/cadp/Crouzen-Lang-11.pdf)。Flordal 与 Malik 也在工业规模有限状态系统上使用候选抽象与启发式顺序进行组合式验证：[论文 DOI](https://doi.org/10.1137/070695526)。

“全自动且返回精确结果的交互同步 FSM 组合最小化/模型检查”至少可追溯到 Chiodo 等人在 1992 年的工作，虽然其目标是性质相关的 CTL 约简，技术对象与 MORPH 不同：[论文原文](https://citeseerx.ist.psu.edu/document?doi=1423ae9c0ec8f5ba28335f3bcf65d117ca9a6b75&repid=rep1&type=pdf)。

从扁平 FSM 自动恢复等价 HFSM 结构也已有系统研究。Biggar、Zamani、Shames 给出了 thin module 的模块分解和 `O(n²k)` 算法：[论文](https://arxiv.org/abs/2111.04902)。

在这些先例之后，MORPH 仍可能有价值的差异化组合是：

1. 对短名单候选实际构造可达组合并求规范最小外部行为商，而不只估算结构可约简性；
2. 把实际商压缩量作为区域形成和下一代搜索的依据；
3. 商机保持开放 Moore 机类型，可递归重新原子化；
4. 同代批量提交互不相交区域；
5. 用成熟三元超边跨越二元中性障碍；
6. 每次安装都执行同态和独立最小性检查。

本次没有找到上述组合完全相同的公开先例，但这只能说明“初步检索未发现”，不能证明新颖性。项目原报告的文献表遗漏了自动精确 CTL 组合约简、Flordal/Malik 系列以及其他自动聚合顺序工作，因此现有相关工作审查不足以支撑首创声明。

## 八、项目价值潜力

### 8.1 已经成立的价值

**第一，语义驱动的模块形成。** 常见模块边界由目录、类、进程或人工层级给出；这里允许运行时以完整可观察行为压缩为依据评价区域。即使这个方向已有组合式验证先例，将“真实最小商的收益”直接变成边界选择信号仍是清楚、有研究价值的设计。

**第二，抽象闭包。** 输出不是一次性的摘要对象，而是原类型机器，因此组合、最小化和验证规则可以递归重复。这种闭包使算法有机会形成真正的多层执行结构，而不是只得到一个分析报告。

**第三，正确性与搜索质量解耦。** EPG 只影响搜索顺序；正确性由证书决定。即使评分启发式很差，只要候选在预算内完成验证，安装仍保持行为。这是可靠系统中很有价值的架构边界。

**第四，超边揭示非二元协同。** 聚合器与任一输入单独组合可能没有压缩，三者一起才出现小商。MORPH-Hyper 把这种“二元局部看不到、三元整体才成立”的现象变成可执行搜索对象，具有进一步发展为一般高阶器官搜索的潜力。

**第五，原型足够紧凑。** 代码规模约 2,500 行，核心接口少，能够完整检查和重跑。对于早期研究，透明度和可审计性比堆叠大型框架更重要。

### 8.2 可能形成高影响的方向

若进一步成功，该路线可能影响以下领域：

- 模型检查：自动形成中间行为商，降低组合顺序对状态爆炸的影响；
- 硬件和协议综合：把经证明等价的局部网络提升为可复用宏状态机；
- 编译器与运行时：从实际接口行为发现可执行宏原语，而不是只靠语法模式；
- 证明携带优化：让每次运行期重写附带小型、可重放的等价证据；
- 复杂系统理论：把“可形成精确小接口商的层级”发展为不同于树宽的复杂性参数。

这些目前都是条件性潜力，包内没有真实程序、工业模型、硬件 IR 或跨领域算法重演证据。

### 8.3 从原型走向重大成果的必要门槛

优先级最高的不是继续增加同类合成树，而是完成以下证据链：

1. **理论化**：严格定义形态宽度或等价参数，证明非平凡复杂性上界、下界或 FPT 结果，并与树宽、接口宽度、通信复杂性区分。
2. **符号化**：用 BDD/AIG/SAT/SMT 表示替代显式真值表，使瓶颈由语义边界而非布尔端口宽度控制。
3. **真正的证明工件**：保存可重放证书，由独立小检查器验证；最好再对检查器或核心合同定理做机械化证明。
4. **公平外部基线**：在相同模型、预算、硬件和成功条件下与 CADP Smart Reduction、Supremica 或其他强基线直接比较。
5. **真实负载**：使用公开协议、硬件控制器、编译器 IR 或数据库计划；测试边界不整齐、含反馈、含噪声结构的网络。
6. **消融实验**：分别移除实际商评分、成熟性、批量提交、超边和预筛选，证明每个新机制的独立贡献。
7. **公开学术锚点**：发布带版本历史和许可证的仓库、预印本、可归档数据及第三方复现说明。

只有在这些环节中至少形成一项强理论结果或跨领域通用系统，项目的“大奖级潜力”才会从叙事变成可检验主张。

## 九、按图灵奖标准判断

ACM 对图灵奖的公开说明要求一项突出、引领趋势的具体技术成就，并要求成功提名给出“持久影响”的清晰证据；参见 [ACM A.M. Turing Award FAQ](https://awards.acm.org/binaries/content/assets/awards/a.m.-turing-award-faq.pdf) 和 [ACM 提名建议](https://awards.acm.org/binaries/content/assets/awards/advice-for-award-nominators-and-endorsers_07-2025.pdf)。

本项目目前缺少图灵奖判断所必需的外部证据：

- 没有经过同行评审的核心论文；
- 没有可验证的作者及贡献归属；
- 没有引用、社区采用或工业部署；
- 没有第三方独立实现或复现；
- 没有一般性理论结果；
- 没有显示其改变一个计算领域的方法论；
- 没有“持久影响”所要求的时间跨度。

所以当前不能把“图灵奖潜力”作为已经通过验证的结论。能通过验证的只是：它提出了一个可能通向重要成果的具体原型，而且这个原型比概念提案更扎实。

## 十、最终价值判断

本项目的含金量来自真实的算法闭环和严格的有限状态局部验证，而不是来自 `2^1023` 之类的大数字。它已经超过普通演示代码：模型边界明确，正确性条件与启发式分离，存在独立最小性检查，实验覆盖逆向可控层级和两个正向生成系统，失败边界也被记录。

其现阶段上限同样明确：实验仍以人工构造的树形网络为主，基线不够强且比较条件有偏，证书没有持久化为可独立验签的工件，相关工作审查遗漏重要先例，项目身份和原创时间没有外部证据。

综合判断：

> **技术真实性：高。研究原型价值：中高。学术新颖性可信度：待验证。工程成熟度：早期。图灵奖级成熟度：当前不具备。长期潜力：取决于能否把“精确自抽象”推进为有新复杂性理论、符号化可扩展实现和跨真实领域影响的通用方法。**
